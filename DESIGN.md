# color-mixer — 設計決議（DESIGN）

> 版本 v0.1｜最後更新 2026-08-07

「怎麼用」歸 [README](README.md)、家族共同規範歸
[nodeapp-webapp-family](https://github.com/scottgfhong310/nodeapp-webapp-family)；本檔只記**為什麼長這樣**。

---

## 1. 這支 app 的兩半，以及它們之間那條線

需求原本是一件事：「調出一個顏色 → 得到它的資訊 → 找家族相近色」＋「替家族的某個色加上一系列**應用校準**色」。

第二半是**寫入端**，而它精準命中兩份早就寫好的文件：

- `faber-castell-color/DESIGN.md` §1.1 的**觸發條件 2**——「出現寫入端：庫存／願望清單／
  **實際上色掃描樣本**（相對於 PDF 取樣的近似 hex），且要與 `color-palette`／`thangka-trace` 共享」。
- `ARTCOLOR_DOMAIN_GOVERNANCE.md` §9-Q5——「那是觸發條件 2，屆時是**另一支私有 app 直連 DB**，
  不開在公開 repo 上」。

**所以 public / private 的分界線不是我們挑的，是它們先寫好的。** 結果是：

```
db_artcolor（建置期 SoR）
   │  ① 校準在這裡寫入 ← 維護介面放 InProgress（private）
   ↓  ② a3-export.js --write（既有管線，多一個匯出目標）
color-mixer（public、零後端）
   ├─ 讀 5 支品牌 registry 的 data/*.js
   └─ 讀 data/calibration.js（唯讀）
```

一個新概念都沒有發明——這正是 v1.8 定下、五支色彩 app 已經在跑的形狀。

**代價要講清楚**：校準值只有 owner 寫得了。公開使用者能調色、能比對、能把配方存進網址列，
但他自己的校準不會進 SoR。要讓任何人都能存，就得有帳號與後端寫入端，那支 app 必然 private——
等於放棄「公開的調色盤」這件事。

---

## 2. 為什麼校準要存**三個** hex

owner 的原始描述是：「B39 在白色 A4 紙上，感覺紙上的顏色**接近 `#08093D` 但較深**」。

`#08093D` 不是觀測值，它是**在螢幕上調到最接近時停下的錨點**；「較深」是明知道還沒調準的方向。
⚠️ 而他的第二筆（宣紙）**錨點一模一樣**——只有修飾語不同。**只存一個 hex，那兩筆在資料上會變成同一筆。**

於是現場有三個 hex，各自回答不同問題：

| | | 回答什麼 | 可跨什麼比較 |
|---|---|---|---|
| ① | `tb_color.fd_hex` | 型錄／色卡值 | — |
| ② | `hexRef` | 目視比對第一次停下的錨點 | — |
| ③ | `hex` | 微調後認可的值 | — |
| | **① → ③** | **「螢幕值 vs 實際上色」的差距** | 跨基材、跨品牌 |
| | **② → ③** | **你的目視偏差** | 跨紀錄，可看有沒有**系統性** |

②→③ 那條特別有意思：**它讓你日後能檢查自己的眼睛**，而那是任何量測都給不了的。

**錨點怎麼被捕捉**：不加額外動作——**第一次按存檔時的值就是 `hexRef`**，之後回頭再改再存只更新 `hex`。
語意剛好是「我第一次認為對的」對「我最後認為對的」。

⚠️ ①→③ 的差距混著兩個成因（印刷失真 ＋ 實際上色）。**先不要試圖拆開**，等累積夠多品牌再看
——若 FC 的差距系統性地小於 COPIC，那就是印刷失真在說話（COPIC 型錄自述「印刷色 ≠ 墨水」）。

### 2.1 行為事實先走自由註記

「沒有渲開」不是顏色資訊，是**行為**（宣紙會渲、A4 不會）。同類還有透背、堆疊起毛、乾後變色。
〔owner 拍板〕**先一律走 `note` 自由文字，不結構化**——等累積夠多、看出哪幾種真的反覆出現，
再抄成欄位。**先不猜。**

護欄很便宜：UI 給幾顆常用詞 chip（`渲開：無`／`渲開：輕微`…），點了插進註記。
不強制、不驗證，只是讓一致變成預設路徑，日後 `LIKE '渲開：%'` 抽得出來。
（同 v1.22 的教訓：`fd_model_manual_at` 想靠 NULL 分辨真人標的與推斷的，結果兩者長得一模一樣。）

---

## 3. 四個混色模型，以及為什麼不是三個

一開始定案是三個（sRGB／OKLab／顏料近似 K-M）。**實測把它推翻了。**

### 3.1 KM 答不出本 app 的核心問題

同一支 COPIC B39（`#08093d`）疊在白紙 `#ffffff` 與宣紙 `#f0e6d2` 上——兩張紙本身差 ΔE00 **10.12**：

| alpha | 0.15 | 0.30 | 0.50 | 0.70 | 0.85 | 0.95 |
|---|---|---|---|---|---|---|
| **glaze** | 7.65 | 5.47 | 2.56 | 1.22 | 0.64 | 0.25 |
| **km** | 0.35 | 0.20 | 0.22 | 0.00 | 0.00 | 0.00 |

**km 在任何濃度下都算不出兩張紙的差別。** 這不是實作 bug，是**模型能力的界線**：
單常數 Kubelka-Munk 描述的是「混合物」，假設無限厚的不透光層，本來就沒有「透光看見基材」這回事
（要有的話得用有厚度的 K-M ＋ Saunderson 修正，需要每個顏料的散射係數，我們沒有）。

而「同一支筆在不同紙上」**正是校準功能在問的問題**。所以加入第四個模型 `glaze`
（Beer-Lambert，光學密度空間插值），並設為預設。

### 3.2 四個模型分兩組，這個分組才是重點

- **光的疊加**：`srgb`（＝瀏覽器行為）、`oklab`
- **減色**：`glaze`（罩染，墨層蓋在基材上）、`km`（調色盤混合，兩坨顏料混在一起）

**km 沒有被拿掉**——「我把兩支顏料混在一起」是真實需求（色鉛筆疊色、水彩調色盤），
它只是不能拿來回答罩染的問題。

### 3.3 減色模型的價值是**彩度**，不是色相

藍 `#2b5fa8` ＋ 黃 `#f2d024` 各半：

| model | hex | L | C（Lab 彩度）| hue |
|---|---|---|---|---|
| srgb | `#8f9866` | 61.0 | 27.9 | 115° |
| oklab | `#8d9c89` | 62.7 | **12.2** | 138° |
| glaze | `#6b8e51` | 55.1 | 37.1 | 130° |
| km | `#3d7833` | 45.2 | **46.3** | 137° |

（輸入自己：藍 C=45.1、黃 C=79.6）

⚠️ **四個模型的色相都落在綠區**——所以「顏料混得出綠」這句話**不能用色相驗**。
`scripts/verify.js` 的 A4 條第一版就是寫成「綠通道是不是最大」，**四個模型全部通過，等於什麼都沒驗到**。
可驗證的形式是：**兩個減色模型的彩度都高於兩個光合成模型**。

⚠️ 同一組數字**推翻了我原本寫的一句文案**：OKLab 的說明本來寫「過渡感知均勻、**中段不發灰**」。
實測 OKLab 是四者中**最灰**的（C=12.2，比 sRGB 的 27.9 更低）——互補色之間走直線會穿過中性軸。
三語文案已改成正確且具體的說法。

### 3.4 一個滑桿，四種語意

| model | alpha 是什麼 |
|---|---|
| srgb / oklab | **覆蓋率**——這一層蓋掉下層多少 |
| glaze | **墨層厚度**——光穿過幾次 |
| km | **濃度**——這一層的顏料佔混合物多少比例 |

四者**兩端點一致**（0＝只有下層、1＝只有這一層，已實測 8/8），中間的性格不同。
**UI 恆顯示現在這一個是什麼意思**——不講的話，使用者換個模型看到數字全變了會以為是 bug。
同 `circle-text`「配平要犧牲哪個變數」做成五選一的理由：**錯的從來不是某個做法，是不講。**

### 3.5 兩個已知限制，寫下來免得日後被「修好」

- **`km` 對純數位原色失真**：`#0000ff` ＋ `#ffff00` → `#161616`（近乎全黑），通道值 0 讓 K/S 爆掉。
  glaze 給 `#474747`（灰，退化得優雅但也不是綠）。真實顏料的 hex 沒有這個問題。
  `verify.js` B3 條把它釘住——若哪天有人「改良」了它，那條會紅，提醒同步改文案。
- **`R_MIN = 0.004` 是我們挑的，不是量的**：反射率下限，相當於「最深的顏料仍反射 0.4% 的光」。
  改它會改變所有深色的混合結果。兩個減色模型共用同一個值，比較才在同一個前提下。

---

## 4. 結構上的幾個決定

### 4.1 控制器不保存任何計算結果

畫面、CSS 匯出、分享連結、最接近色，**四個出口各自由當下的 `state` 重算**。
代價是幾十次浮點運算，換到的是「畫面與輸出不一致」這個 bug 類別**在結構上不存在**
（`circle-text` 的教訓，家族 v1.17：原型只有 3/8 個欄位觸發重算，改了總字數按複製，出去的是舊值）。

### 4.2 lib 不重寫五個品牌的比對規矩

本 lib **沒有** `nearestAcross`，只有 `mergeNearest`。因為：

| 品牌 | 它自己的規矩 |
|---|---|
| Faber-Castell | 預設只比 `ag`，不含 Black Edition（另一條產品線） |
| Caran d’Ache | 排除 PSTC（與 PSTP 共用同一份官方調色盤） |
| COPIC | 排除無色調和筆 |
| Finecolour | 預設只比 marker 色號空間，不含彩針筆與壓克力筆 |
| ENMY | 收錄不明的套組**忽略該過濾條件而不是回空陣列** |

在這裡重寫一份就是「同一條規則有第二份實作」（v1.16）。
連「現在比的是幾支筆」也是**問各品牌自己**（`near(probe, 1e6).length`），不在控制器重數一遍
——實測 **1,779 支**（不是全部 2,157），這個數字恆顯示在畫面上。

### 4.3 品牌鍵＝app 資料夾名，且匯出器要負責對照

⚠️ `db_artcolor.meta_brand.fd_code` 與 app 資料夾名**五個全都不一樣**：

| `fd_code` | app 資料夾名（本 app 與 URL 用這個） |
|---|---|
| `faber-castell` | `faber-castell-color` |
| `caran-dache` | `caran-dache-color` |
| `copic` | `copic-color` |
| `finecolour` | `finecolour-color` |
| `enmy` | `enmy-color` |

理由：本 app 消費的是**五支 app 的資料檔**，不是 DB；`data-brand`、`pool.<id>` 的 i18n key、
比對池統計全都以資料夾名為鍵（家族「命名一致」canon）。

**匯出器要做這層對照，且要用明列的對照表、不要用字串接尾**——目前五支剛好都是 `<fd_code>-color`，
但第六個品牌不保證（STAEDTLER 已滿足進庫條件，app 名還沒定）。

### 4.4 網址列＝存檔，所以它必須手改得動

`?m=glaze&b=f0e6d2&s=xuan-natural&l=08093d.55.copic-color~B39_e8c33a.30`

刻意**不用 JSON＋base64**：網址要看得懂、手改得動、diff 得出來（同 `circle-text`）。
代價是 brand 打錯必然會發生——所以認不出來時要明講「未知品牌」。

⚠️ 這不是假想的：測試時手打了 `copic`（而非 `copic-color`），畫面顯示成 **`copic B39`**，
**看起來與正確的「COPIC B39」一樣是對的**。

---

## 5. 驗證時踩到的三個坑（都不會報錯）

1. **假校準資料用了 `brand: 'copic'`** → `calibratedColors()` **一筆都沒換、也沒報錯**，
   而畫面上「以校準值比對」的勾選框照樣勾得起來。→ `verify.js` D4 條（參照完整性）。

2. **`calibrated` 旗標活不過品牌 lib**：`calibratedColors()` 掛的 `calibrated: true`，
   在 `nearestXxx` 把結果**投影**成自己那組固定欄位時被丟掉。症狀是
   **色片用了校準值、徽章卻不亮**。→ 加 `hasCalibration()`，控制器改問它；
   `verify.js` D5 條同時擋「控制器不得再讀那個旗標」。

3. **D5 的第一版誤中自己寫的註解**：掃全檔找 `it.calibrated`，命中的是提醒「不可以看它」的那行警語，
   於是檢查恆紅。→ 掃原始碼找「有沒有用到某個東西」時**必須先剝掉註解與字串**。

另有兩次 **preview pane 背景假象**（家族 v1.17 記過五種）：
`visibilityState === 'hidden'` 時 `transition` 凍結在起點，量到 canvas 背景是 `transparent`
（釘掉 transition 立刻量到 `rgb(255,255,255)`）；`innerWidth === 0` 讓版面量到 130px。
**下結論前先確認分頁是不是前景。**

還有一次**我自己的測法錯了**：用靜態 NodeList 逐一 `click()` 四顆模型 chip，
但 `renderModels()` 每次會整組重繪，第一次點擊後其餘元素已脫離 DOM、委派 handler 收不到
——量到「四個模型結果相同」，看起來像模型沒生效。**每次重新查詢元素後，四個模型各給不同結果。**

---

## 6. 尚未做的事

- **canvas 筆刷塗抹**（階段二）。刻意排在色塊疊加之後：筆刷是 DOM 工作、進不了 lib（§4.1／§4.7），
  而它消費的正是階段一算出來的結果色，**lib 一行都不用改**。反過來做會讓合成邏輯長在繪圖迴圈裡，
  於是「畫面看到的顏色」與「複製出去的 hex」變成兩條路徑——正是 §4.1 要避免的那件事。
- **校準的正式資料模型**進 `ARTCOLOR_DOMAIN_GOVERNANCE.md`（新的 §3.x），含第三類來源
  `measured` 的定位。現在 `data/calibration.js` 是**佔位資料**，`CM_CALIBRATION_META.stub`
  為 true 時畫面掛告示（`verify.js` F3 條擋著它不准被拿掉）。
- **`color-metric.js` 共用件**：五支品牌 lib ＋ 本支的色彩科學核心逐字相同、共六份複製。
  抽出來是對的（比照 v1.18 的 `color-family.js`），但那是一次跨 6 個 repo 的收斂，
  **刻意不混進第一版**（v1.17／v1.18 記過兩次「commit 訊息與內容對不起來」）。
- ~~**icons/ 與 favicon set**~~ **已完成**（2026-08-08）：`scripts/make-icons.py` 產整套，
  `.ico` 走家族 `tools/make-ico.sh`（**不可 `cp`**）。標記是**兩片色重疊、交集是減色的結果**
  ——與另五支色彩 app 區隔：那些描述「一批色」，本 app 描述「兩色相遇會怎樣」。
  ⚠️ **重疊色不寫死，由本 app 的 lib 現算**（比照 `copic-color` 的 make-icons.py「九格由資料現查」）：
  模型改了 icon 跟著改，`verify.js` F10 條第 ⑤ 項比對兩者是否仍相同。
  ⚠️ **交集是畫上去的第三個形狀，不是用 `fill-opacity` 疊的**——瀏覽器的 alpha 合成給的是
  灰橄欖 `#8f9866`，減色才給得出綠 `#3d7833`；用 opacity 疊等於這枚 icon 在反駁自己。
- 發佈（`gh repo create`）、家族 README 成員表、`app-launcher` registry、i18n 盤點匯入。

---

*MIT © 2026 Scott G.F. Hong*
