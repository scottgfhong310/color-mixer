# color-mixer — 設計決議（DESIGN）

> 版本 v0.3｜最後更新 2026-08-09

「怎麼用」歸 [README](README.md)、家族共同規範歸
[nodeapp-webapp-family](https://github.com/scottgfhong310/nodeapp-webapp-family)；本檔只記**為什麼長這樣**。

---

## 1. 這支 app 的兩半，以及它們之間那條線

需求原本是一件事：「調出一個顏色 → 得到它的資訊 → 找家族相近色」＋「替家族的某個色加上一系列**應用校準**色」。

第二半是**寫入端**，而它精準命中兩份早就寫好的文件：

- `faber-castell-color/DESIGN.md` §1.1 的**觸發條件 2**——「出現寫入端：庫存／願望清單／
  **實際上色掃描樣本**（相對於 PDF 取樣的近似 hex），且要與 `color-palette`／`thangka-trace` 共享」。
- `ARTCOLOR_DOMAIN_GOVERNANCE.md` §9-Q5——「那是觸發條件 2，屆時是**另一支私有 app 直連 DB**，
  不開在公開 repo 上」。

**所以 public / private 的分界線不是我們挑的，是它們先寫好的。** 結果是：

```
db_artcolor（建置期 SoR）
   │  ① 校準在這裡寫入 ← 維護介面放 InProgress（private）
   ↓  ② a3-export.js --write（既有管線，多一個匯出目標）
color-mixer（public、零後端）
   ├─ 讀 5 支品牌 registry 的 data/*.js
   └─ 讀 data/calibration.js（唯讀）
```

一個新概念都沒有發明——這正是 v1.8 定下、五支色彩 app 已經在跑的形狀。

**代價要講清楚**：校準值只有 owner 寫得了。公開使用者能調色、能比對、能把配方存進網址列，
但他自己的校準不會進 SoR。要讓任何人都能存，就得有帳號與後端寫入端，那支 app 必然 private——
等於放棄「公開的調色盤」這件事。

---

## 2. 為什麼校準要存**三個** hex

owner 的原始描述是：「B39 在白色 A4 紙上，感覺紙上的顏色**接近 `#08093D` 但較深**」。

`#08093D` 不是觀測值，它是**在螢幕上調到最接近時停下的錨點**；「較深」是明知道還沒調準的方向。
⚠️ 而他的第二筆（宣紙）**錨點一模一樣**——只有修飾語不同。**只存一個 hex，那兩筆在資料上會變成同一筆。**

於是現場有三個 hex，各自回答不同問題：

| | | 回答什麼 | 可跨什麼比較 |
|---|---|---|---|
| ① | `tb_color.fd_hex` | 型錄／色卡值 | — |
| ② | `hexRef` | 目視比對第一次停下的錨點 | — |
| ③ | `hex` | 微調後認可的值 | — |
| | **① → ③** | **「螢幕值 vs 實際上色」的差距** | 跨基材、跨品牌 |
| | **② → ③** | **你的目視偏差** | 跨紀錄，可看有沒有**系統性** |

②→③ 那條特別有意思：**它讓你日後能檢查自己的眼睛**，而那是任何量測都給不了的。

**錨點怎麼被捕捉**：不加額外動作——**第一次按存檔時的值就是 `hexRef`**，之後回頭再改再存只更新 `hex`。
語意剛好是「我第一次認為對的」對「我最後認為對的」。

⚠️ ①→③ 的差距混著兩個成因（印刷失真 ＋ 實際上色）。**先不要試圖拆開**，等累積夠多品牌再看
——若 FC 的差距系統性地小於 COPIC，那就是印刷失真在說話（COPIC 型錄自述「印刷色 ≠ 墨水」）。

### 2.1 行為事實先走自由註記

「沒有渲開」不是顏色資訊，是**行為**（宣紙會渲、A4 不會）。同類還有透背、堆疊起毛、乾後變色。
〔owner 拍板〕**先一律走 `note` 自由文字，不結構化**——等累積夠多、看出哪幾種真的反覆出現，
再抄成欄位。**先不猜。**

護欄很便宜：UI 給幾顆常用詞 chip（`渲開：無`／`渲開：輕微`…），點了插進註記。
不強制、不驗證，只是讓一致變成預設路徑，日後 `LIKE '渲開：%'` 抽得出來。
（同 v1.22 的教訓：`fd_model_manual_at` 想靠 NULL 分辨真人標的與推斷的，結果兩者長得一模一樣。）

---

## 3. 四個混色模型，以及為什麼不是三個

一開始定案是三個（sRGB／OKLab／顏料近似 K-M）。**實測把它推翻了。**

### 3.1 KM 答不出本 app 的核心問題

同一支 COPIC B39（`#08093d`）疊在白紙 `#ffffff` 與宣紙 `#f0e6d2` 上——兩張紙本身差 ΔE00 **10.12**：

| alpha | 0.15 | 0.30 | 0.50 | 0.70 | 0.85 | 0.95 |
|---|---|---|---|---|---|---|
| **glaze** | 7.65 | 5.47 | 2.56 | 1.22 | 0.64 | 0.25 |
| **km** | 0.35 | 0.20 | 0.22 | 0.00 | 0.00 | 0.00 |

**km 在任何濃度下都算不出兩張紙的差別。** 這不是實作 bug，是**模型能力的界線**：
單常數 Kubelka-Munk 描述的是「混合物」，假設無限厚的不透光層，本來就沒有「透光看見基材」這回事
（要有的話得用有厚度的 K-M ＋ Saunderson 修正，需要每個顏料的散射係數，我們沒有）。

而「同一支筆在不同紙上」**正是校準功能在問的問題**。所以加入第四個模型 `glaze`
（Beer-Lambert，光學密度空間插值），並設為預設。

### 3.2 四個模型分兩組，這個分組才是重點

- **光的疊加**：`srgb`（＝瀏覽器行為）、`oklab`
- **減色**：`glaze`（罩染，墨層蓋在基材上）、`km`（調色盤混合，兩坨顏料混在一起）

**km 沒有被拿掉**——「我把兩支顏料混在一起」是真實需求（色鉛筆疊色、水彩調色盤），
它只是不能拿來回答罩染的問題。

### 3.3 減色模型的價值是**彩度**，不是色相

藍 `#2b5fa8` ＋ 黃 `#f2d024` 各半：

| model | hex | L | C（Lab 彩度）| hue |
|---|---|---|---|---|
| srgb | `#8f9866` | 61.0 | 27.9 | 115° |
| oklab | `#8d9c89` | 62.7 | **12.2** | 138° |
| glaze | `#6b8e51` | 55.1 | 37.1 | 130° |
| km | `#3d7833` | 45.2 | **46.3** | 137° |

（輸入自己：藍 C=45.1、黃 C=79.6）

⚠️ **四個模型的色相都落在綠區**——所以「顏料混得出綠」這句話**不能用色相驗**。
`scripts/verify.js` 的 A4 條第一版就是寫成「綠通道是不是最大」，**四個模型全部通過，等於什麼都沒驗到**。
可驗證的形式是：**兩個減色模型的彩度都高於兩個光合成模型**。

⚠️ 同一組數字**推翻了我原本寫的一句文案**：OKLab 的說明本來寫「過渡感知均勻、**中段不發灰**」。
實測 OKLab 是四者中**最灰**的（C=12.2，比 sRGB 的 27.9 更低）——互補色之間走直線會穿過中性軸。
三語文案已改成正確且具體的說法。

### 3.4 一個滑桿，四種語意

| model | alpha 是什麼 |
|---|---|
| srgb / oklab | **覆蓋率**——這一層蓋掉下層多少 |
| glaze | **墨層厚度**——光穿過幾次 |
| km | **濃度**——這一層的顏料佔混合物多少比例 |

四者**兩端點一致**（0＝只有下層、1＝只有這一層，已實測 8/8），中間的性格不同。
**UI 恆顯示現在這一個是什麼意思**——不講的話，使用者換個模型看到數字全變了會以為是 bug。
同 `circle-text`「配平要犧牲哪個變數」做成五選一的理由：**錯的從來不是某個做法，是不講。**

### 3.5 兩個已知限制，寫下來免得日後被「修好」

- **`km` 對純數位原色失真**：`#0000ff` ＋ `#ffff00` → `#161616`（近乎全黑），通道值 0 讓 K/S 爆掉。
  glaze 給 `#474747`（灰，退化得優雅但也不是綠）。真實顏料的 hex 沒有這個問題。
  `verify.js` B3 條把它釘住——若哪天有人「改良」了它，那條會紅，提醒同步改文案。
- **`R_MIN = 0.004` 是我們挑的，不是量的**：反射率下限，相當於「最深的顏料仍反射 0.4% 的光」。
  改它會改變所有深色的混合結果。兩個減色模型共用同一個值，比較才在同一個前提下。

---

## 3.6 反解（拆色）：為什麼它解得動，以及它拒絕回答什麼

起點是一句實際的抱怨：**「以 B39 為基礎調濃度，怎麼樣都得不到我在紙上畫出來的顏色。」**
量了之後那不是技巧問題，是**目標在單支筆的可達集合之外**——而這件事能一句話證明：

| | L\* | 與 B39 型錄色 `#255da7` 的 ΔE00 |
| --- | --- | --- |
| B39 型錄色 | 39.6 | — |
| 紙上目視 `#171159` | 11.3 | **25.2** |

濃度只能在「紙色 → 型錄色」之間插值，目標比型錄色暗 28 個 L\*，**四個模型全部**最近只到
ΔE00 25.2，而且都停在滿濃度。⚠️ 那個 25.2 正是治理文件早就寫著的東西——COPIC 的 hex
來自型錄，型錄自述「印刷色 ≠ 墨水」，庫裡標 `verify_level = approximate`。
**所以這一題其實屬於 §2 的校準，不是調色**；實測校準值 `#06072f` 一進來，
同一支筆單獨用就從 ΔE00 25.22 掉到 **7.37**（濃度 81%）。

### 為什麼是凸問題

四個模型的差別**只在於「在哪個空間裡做線性插值」**（`SPACE` 那張表），於是逐層疊加恆等於
該空間裡的一個**凸組合**：

    V_out = w_base·V_base + Σ w_i·V_i ，  w_base = Π(1−a_i)、w_i = a_i·Π_{j>i}(1−a_j)、Σw = 1

所以「這個調色盤調得出哪些顏色」＝ 這些點的**凸包**，反解＝求目標到凸包的最近點。
有唯一最佳解，**而且到不了時能證明到不了**，不是「再試試看」。

⚠️ **`srgb`／`glaze`／`km` 精確成立，`oklab` 不成立**（各 4,000 組隨機堆疊實測：前三者最大
ΔE00 = 0.0000，oklab 是 1.46／反向 3.06——它的逐層結果會被夾回 sRGB 色域）。
`EXACT_CONVEX` 這張表是**量出來的、不是推導出來的**，`verify.js` G1 兩個方向都擋著。

### 三個踩過的坑

1. **空間裡的最近點 ≠ ΔE00 最近。** 凸包只負責回答「可不可達」；不可達時的「最近」必須
   改用真正的 ΔE00 再微調一次，否則報出來的殘差不是最小值——而**殘差是這個功能唯一的產出**。
2. ⚠️ **微調前不可以先剔除權重為 0 的顏料**，剔了就再也加不回來。km 尤其嚴重（通道近 0 時
   K/S 衝到 124，座標軸尺度差百倍），實測有四組把某支顏料整個丟掉、殘差 2.4–11.3。
3. ⚠️ **只走單軸的座標下降會卡在「兩支顏料互相取代」那條對角谷裡**（實測停在 ΔE00 2.37）。
   加上成對移動（一支加、一支減）之後同一組收斂到 <1。**這種卡住沒有任何徵兆**：
   它回一個合理的配方與一個看起來還行的殘差。

### 它拒絕回答什麼

- **`reachable` ＝ 算出來的顏色與目標在 8 位元下同一個 hex**，不是「ΔE00 小於某個數」。
  試過用凸包距離加容差，錯的：目標 hex 本身已量化，實測 300 組往返有 13–20% 被判成不可達
  ——**假陰性**，而「到不了」是這個功能最重的一句話。也不用 ΔE00 門檻，那個數會是憑感覺挑的。
- **標籤只回答「有沒有剛好命中」，不回答「像不像」**。ΔE00 與色差級距另外顯示——
  第一版寫成「調不出來」，配上 ΔE00 1.94「極接近」自相矛盾，而且**把話講得比事實嚴重**
  （家族 v1.18：誇大的敘述會變成下一個人的錯誤前提）。
- **模型說疊層順序無關**（凸組合天生如此），真紙上不是。這是模型的界線，畫面要講。

### 內建調色盤，與唯一一條校準進得來的路

`rgb`（預設）／`cmy`／`cmyk`／`layers`／`custom`。RGB 當預設不是因為減色情境下三原色顏料
物理上正確（疊起來只會發濁），而是**三根滑桿、心裡有數**；而且實測在預設模型下它剛好也最準
（目標 `#171159`：rgb 1.94／cmyk 5.30／cmy 20.65）。**`cmy` 留著是為了讓「少一個 K」看得見**，
不是為了推薦它——「CMY 調不出深藍」是調色盤少一個維度，不是使用者技巧不好。

⚠️ **`layers`（畫布上現有的顏料層）是校準值唯一進得到反解的路徑**：抽象原色與手填 hex 都沒有
「哪支筆」這個身分，也就無從校準；層帶著 `src`（brand＋code），所以勾了「以校準值比對」
又選了基材時，算的才是**紙上的**配方。它同時回答最實用的那個問題——「我手上就這幾支筆，
濃度該調多少」。

⚠️ 上線時踩到一個既有 handler 的失效：`#use-calib` 原本只呼叫 `renderNear()`——當年
`useCalib` 只有最接近色在讀，**完全正確**。反解多了第二個讀者之後，症狀是**勾選框勾了沒反應**，
不報錯、不當機，反解安靜地繼續用型錄色。**「記得在每個出口補呼叫」是會過期的紀律**，
已改成 `renderAll()` 並寫成 `verify.js` G7。

## 3.7 目視不精確是前提，不是缺陷

Owner 指出的三件事——工作區的光線、螢幕的品質與色準、個人對色彩的敏銳度——都是真的，
而且沒有一個是這支 app 修得掉的。**但它們不會讓這件事失去意義，因為記的本來就不是「墨水是什麼顏色」。**

### 重新定義：主觀值的精確紀錄，不是客觀值的近似

存的是「**在這個螢幕上放什麼值，看起來才像我當時看到的**」。這樣一講，三個變因全都在
框架**之內**、消費時互相抵消——只要在同一個框架裡消費它。而 app 就是這樣用它的（渲染那個圓）。
Owner 的原話是「接近**一個人對於顏色的表達**」；它比那更強一點，**它就是那個表達本身**。

### 這個不精確會不會改變 app 給的答案：量過，不會

以指定的 ΔE00 半徑在目標周圍隨機取方向擾動 400 次，看「最接近的筆」怎麼變。
⚠️ **該看的不是名次有沒有變，是換到的那支筆長得多不一樣**：

| | 目標 `#171159`（稀疏區） | 目標 `#255da7`（密集區） |
| --- | --- | --- |
| 誤差 ΔE00 1 | 第 1 名 **0%** 換人 | 40% 換人，但只差 **1.47** |
| 誤差 ΔE00 3 | **0%** 換人 | **100%** 換人，但只差 **2.97** |
| 誤差 ΔE00 5 | 16% 換人，一換就差 **12.37** | 100% 換人，差 4.91 |

**你的誤差 1:1 傳遞過去，從不放大**——ΔE00 3 的目視誤差換到 ΔE00 3 不同的筆，那已是理論下限。
密集區裡「100% 換人」聽起來很糟，但換到的筆看起來一樣；**名次翻動不代表答案變了**。
⚠️ 例外是稀疏區的**懸崖**：小誤差完全免疫，一旦超過第 1、2 名的差距就跳很遠。
而**那個差距 app 一直都知道**，只是沒說——現在說了（`renderRobust`），並附上兩種讀法。

### 準確度驗不了，一致性可以

`meta_observe_context`（db_artcolor CHG000050）＝ 光線＋螢幕＋觀察者的一個**具名組合**。
⚠️ **刻意不是三個可校正的欄位**——我們沒有分光儀也沒有螢幕特性化，那三個因素一個都校正不了。
唯一問得出也唯一有用的問題是**「這兩次觀測是不是在同一個框架下做的」**，那是 identity 不是測量。

於是離散度分成兩個**不可混談**的數字（`calibrationSummary`）：

- **`repeatability`（同框架）** ＝ 你自己的精度。**唯一一個不需要真值就量得出來的誤差棒**，
  而且它不是對墨水的宣稱。
- **`frameGap`（跨框架）** ＝ 換了光線／螢幕的結果。那是另一個問題的答案。

⚠️ 兩者加在一起平均會得到一個什麼都不是的數字。`verify.js` H2 直接擋「這兩個數字相等」。
⚠️ 只有一次觀測時兩者都回 `null` **而不是 0**——0 會被讀成「量過，而且非常一致」。
⚠️ 框架未記錄（`context` 為空）的列**各自成組、不互相合併**：「沒記錄」不等於「同框架」。

### 這件事推翻了昨天才寫進 DB 的一個假設

`rel_color_substrate` 原本帶著 `UNIQUE (fd_color_idx, fd_substrate_idx, fd_layers)`——
那是一句斷言：「這支筆、這張紙、這個層數，有唯一一個真值。」上面那段話正是它不成立。
CHG000050 拿掉它（同三欄改建一般索引），因為**同一個框架下把同一件事記兩次正是要支援的動作**。

⚠️ 與 §9-Q7（Finecolour 證偽 `fd_name_en NOT NULL`）同型：**schema 的假設不只在表結構裡，
也在約束裡，而約束更安靜**——它不會回傳錯的資料，它會讓對的資料**存不進來**。
⚠️ 拿掉之後，靠它成立的假設全部失效而且不報錯：`calibratedColors()` 原本「層數最少者勝」，
同層數只有一列所以無歧義，現在會**任意**挑一列。已改為走 `calibrationSummary` 聚合。

### 把「說出顏色」換成「調到看起來像」

三個困難全是**絕對判斷**（這是什麼顏色）。人在**相對判斷**上強得多，所以目視色多了一條路：
給一個**錨點**，再用三根滑桿說出它「但是⋯⋯」的部分（明度／彩度／色相）。
這直接對應 owner 自己的講法——「接近 `#08093D` **但較深**」不是一個 hex，是錨點加方向。
錨點存成 `fd_hex_ref`、結果存成 `fd_hex`，網址參數 `a=`。

⚠️ **它改善的是輸入方式，不是消除光線與螢幕的誤差。** 那是拿螢幕比紙、跨介質，抵消不了；
只有「螢幕比螢幕」的兩塊色才會抵消。文件裡一度把這句話講得比事實強，已改回。

⚠️ **滑桿位置不存 state，一律由 `describeNudge(錨點, 目視色)` 現算**，所以畫面顯示的恆是**實得值**。
兩個踩到的坑：

1. **中性色的色相是 `atan2(0,0)` 的產物**：灰色轉一下色相會回報 `dh = −63.2` 這種垃圾值。
   `describeNudge` 回 `hueDefined:false`，UI 停用那根滑桿並說明。
2. ⚠️ **色域到頂偵測不出來，只能在事件當下記。** 第一版用「沿該軸再推一步、看顏色變不變」猜，
   錯在**色域邊界不是硬牆**——越推越靠近角落，每一步都還會變一點點。實測要求 L−40、
   實得 −2.1 的情形探針照樣「有變」，**警示一次都沒出現過**。
   要求值是**輸入**、不是算得出來的東西；猜不到就別猜，改在滑桿事件裡比對後記下來。

## 4. 結構上的幾個決定

### 4.1 控制器不保存任何計算結果

畫面、CSS 匯出、分享連結、最接近色，**四個出口各自由當下的 `state` 重算**。
代價是幾十次浮點運算，換到的是「畫面與輸出不一致」這個 bug 類別**在結構上不存在**
（`circle-text` 的教訓，家族 v1.17：原型只有 3/8 個欄位觸發重算，改了總字數按複製，出去的是舊值）。

### 4.2 lib 不重寫五個品牌的比對規矩

本 lib **沒有** `nearestAcross`，只有 `mergeNearest`。因為：

| 品牌 | 它自己的規矩 |
|---|---|
| Faber-Castell | 預設只比 `ag`，不含 Black Edition（另一條產品線） |
| Caran d’Ache | 排除 PSTC（與 PSTP 共用同一份官方調色盤） |
| COPIC | 排除無色調和筆 |
| Finecolour | 預設只比 marker 色號空間，不含彩針筆與壓克力筆 |
| ENMY | 收錄不明的套組**忽略該過濾條件而不是回空陣列** |

在這裡重寫一份就是「同一條規則有第二份實作」（v1.16）。
連「現在比的是幾支筆」也是**問各品牌自己**（`near(probe, 1e6).length`），不在控制器重數一遍
——實測 **1,779 支**（不是全部 2,157），這個數字恆顯示在畫面上。

### 4.3 品牌鍵＝app 資料夾名，且匯出器要負責對照

⚠️ `db_artcolor.meta_brand.fd_code` 與 app 資料夾名**五個全都不一樣**：

| `fd_code` | app 資料夾名（本 app 與 URL 用這個） |
|---|---|
| `faber-castell` | `faber-castell-color` |
| `caran-dache` | `caran-dache-color` |
| `copic` | `copic-color` |
| `finecolour` | `finecolour-color` |
| `enmy` | `enmy-color` |

理由：本 app 消費的是**五支 app 的資料檔**，不是 DB；`data-brand`、`pool.<id>` 的 i18n key、
比對池統計全都以資料夾名為鍵（家族「命名一致」canon）。

**匯出器要做這層對照，且要用明列的對照表、不要用字串接尾**——目前五支剛好都是 `<fd_code>-color`，
但第六個品牌不保證（STAEDTLER 已滿足進庫條件，app 名還沒定）。

### 4.4 網址列＝存檔，所以它必須手改得動

`?m=glaze&b=f0e6d2&s=xuan-natural&l=08093d.55.copic-color~B39_e8c33a.30`

刻意**不用 JSON＋base64**：網址要看得懂、手改得動、diff 得出來（同 `circle-text`）。
代價是 brand 打錯必然會發生——所以認不出來時要明講「未知品牌」。

⚠️ 這不是假想的：測試時手打了 `copic`（而非 `copic-color`），畫面顯示成 **`copic B39`**，
**看起來與正確的「COPIC B39」一樣是對的**。

---

### 4.8 筆刷：合成一次都不准發生在繪圖迴圈裡

階段二排在色塊疊加之後，理由寫在原本的待辦裡：筆刷是 DOM 工作、進不了 lib（§4.1／§4.7），
而它消費的正是階段一算出來的結果色。反過來做會讓合成邏輯長在繪圖迴圈裡，於是
「畫面看到的顏色」與「複製出去的 hex」變成兩條路徑。**做出來之後 lib 確實一行都沒改**
——只是**加**了一支 `paintTable()`。

**作法：筆刷不做合成，只數次數。** 控制器維護一個 `Uint8Array`——「這個像素被塗過幾次」——
顏色一律查 `Lib.paintTable(stack, model, n)`：

    t[0] = 基材紙色
    t[n] = compose({ base: t[n−1], layers }, model)     ← 一筆 ＝ 把整個堆疊再蓋一次

於是三件事同時成立：

1. **`t[1]` 恆等於圓圈的結果色**（四模型實測相符，`verify.js` I1 擋著）。
   「一筆 ＝ 圓圈那個顏色」是使用者唯一需要記住的心智模型。
2. **換模型／換基材／改顏料層，同一批筆觸立刻改頭換面**——因為只要重算那張表。
   模型差異於是在**重疊處**直接看得見：同樣兩筆交叉，
   `srgb` 給 `#4877b5`、`glaze` 給 `#346eb3`、`km` 給 `#2963ad`（km 上色最快）。
   **這是圓圈做不到的事**——圓圈一次只顯示一個顏色，筆刷同時顯示 0～n 筆的整條序列。
3. 合成只發生在 `paintTable` 裡，而它用的是同一個 `compose`。**沒有第二份實作**（v1.16）。

⚠️ **最誘人的捷徑是用 canvas 的 `globalAlpha` 疊，而那是錯的**：那是瀏覽器的 alpha 合成，
等於**永遠只有 `srgb` 一個模型**，而畫面上還寫著現在選的是「罩染」。
它不報錯、不當機，只會讓四個模型看起來一模一樣。`verify.js` I2 明文擋著這個字。

### 其餘幾個不明顯的決定

- **塗出來的東西不是 state，不進網址。** 網址存的是配方（可分享、可重現），塗鴉是一塊試色布。
  所以 §4.1 仍然成立：圓圈／CSS／分享連結／最接近色四個出口，**一個都不看那塊畫布**（I4 擋著）。
- **同一筆之內，同一個像素只加一次。** 否則慢慢拖過同一點就會越拖越深，
  而「塗了幾筆」這個語意就沒了。實作是每筆一個 `stroke` 遮罩（實測：同一筆反覆停留仍是 `t[1]`，
  分兩筆點同一處才變 `t[2]`）。
- **`.paint` 預設 `pointer-events: none`**，只有塗抹模式打開。否則它蓋住整個基材、
  把圓圈的互動全吃掉，**而畫面看起來完全正常**（I3 擋著）。
- **塗抹時圓圈縮到左上角當色片，但不整個藏起來**——它是「現在這一筆是什麼顏色」的唯一憑據。
- **重設尺寸時已塗的內容等比保留**，不清畫；`getBoundingClientRect()` 量到 0 時直接不動
  （preview pane 在背景會這樣，§5 那五種假象之一；配置 0×0 緩衝區會讓之後每次塗抹靜默失敗）。
- **`pointerup` 之外還要收 `pointercancel`**：拖到一半被系統打斷會讓 `last` 卡住，
  下一次按下去會從舊座標拉一條線過來。

## 5. 驗證時踩到的三個坑（都不會報錯）

1. **假校準資料用了 `brand: 'copic'`** → `calibratedColors()` **一筆都沒換、也沒報錯**，
   而畫面上「以校準值比對」的勾選框照樣勾得起來。→ `verify.js` D4 條（參照完整性）。

2. **`calibrated` 旗標活不過品牌 lib**：`calibratedColors()` 掛的 `calibrated: true`，
   在 `nearestXxx` 把結果**投影**成自己那組固定欄位時被丟掉。症狀是
   **色片用了校準值、徽章卻不亮**。→ 加 `hasCalibration()`，控制器改問它；
   `verify.js` D5 條同時擋「控制器不得再讀那個旗標」。

3. **D5 的第一版誤中自己寫的註解**：掃全檔找 `it.calibrated`，命中的是提醒「不可以看它」的那行警語，
   於是檢查恆紅。→ 掃原始碼找「有沒有用到某個東西」時**必須先剝掉註解與字串**。

另有兩次 **preview pane 背景假象**（家族 v1.17 記過五種）：
`visibilityState === 'hidden'` 時 `transition` 凍結在起點，量到 canvas 背景是 `transparent`
（釘掉 transition 立刻量到 `rgb(255,255,255)`）；`innerWidth === 0` 讓版面量到 130px。
**下結論前先確認分頁是不是前景。**

還有一次**我自己的測法錯了**：用靜態 NodeList 逐一 `click()` 四顆模型 chip，
但 `renderModels()` 每次會整組重繪，第一次點擊後其餘元素已脫離 DOM、委派 handler 收不到
——量到「四個模型結果相同」，看起來像模型沒生效。**每次重新查詢元素後，四個模型各給不同結果。**

---

## 6. 尚未做的事

> ⚠️ **這一節逐項實查於 2026-08-09。** 原本八項裡有七項早已完成而清單沒跟上——
> 那是家族 v1.15 補記三記過的「清單落後實作」，而且**結轉的動作本身看起來像確認**。
> 判準：**列一條待辦之前先查它還開著沒有，查不動就別列。**

~~**canvas 筆刷塗抹**（階段二）~~ **2026-08-09 完成**，見 §4.8。清單目前是空的。

### 已完成（保留紀錄，因為它們各自留下了判準）

- ~~**校準的正式資料模型**~~ 2026-08-08 進 `ARTCOLOR_DOMAIN_GOVERNANCE.md` **§3.5**
  （CHG000049），含第三類來源 `measured` 與方法 `visual-match`。
  隔天 CHG000050 又**拿掉了它的唯一約束**——目視值取決於光線／螢幕／觀察者，
  「一組（筆, 基材, 層數）有唯一真值」這句斷言不成立（見 §2 與該 SQL 的檔頭）。
  `data/calibration.js` 自 2026-08-08 起是 `db_artcolor` 的匯出產物，`stub` 為 false；
  stub 那條路徑與 `verify.js` F3 保留，下次真的要放佔位資料時還會用到。
- ~~**`color-metric.js` 共用件**~~ 2026-08-08 抽出（權威版在家族 repo 根，17 個複製點）。
  ⚠️ 抽出時實查發現「六份逐字相同」**當時已經不成立**——四個函式分兩派，其中 `hexToRgb`
  是真的行為差異（一派壞輸入回 null、一派永不回 null）。統一為嚴格版，順帶修好了
  `color-palette`／`thangka-trace` 裡 10 個守衛只有 4 個有效的既有 bug。
- ~~**icons/ 與 favicon set**~~ 2026-08-08：`scripts/make-icons.py` 產整套，
  `.ico` 走家族 `tools/make-ico.sh`（**不可 `cp`**）。標記是**兩片色重疊、交集是減色的結果**
  ——與另五支色彩 app 區隔：那些描述「一批色」，本 app 描述「兩色相遇會怎樣」。
  ⚠️ **重疊色不寫死，由本 app 的 lib 現算**（比照 `copic-color` 的「九格由資料現查」）：
  模型改了 icon 跟著改，`verify.js` F10 第 ⑤ 項比對兩者是否仍相同。
  ⚠️ **交集是畫上去的第三個形狀，不是用 `fill-opacity` 疊的**——瀏覽器的 alpha 合成給的是
  灰橄欖 `#8f9866`，減色才給得出綠 `#3d7833`；用 opacity 疊等於這枚 icon 在反駁自己。
- ~~**發佈與家族登錄**~~ 全部完成（逐項實查）：`gh repo create` public ✔／
  家族 README 成員表 ✔（2026-08-09 順帶修掉該表五處過期敘述，並補上
  `tools/test-readme-counts.js` 收卷）／`app-launcher` registry ✔（`apps.js` 第 281 列）／
  **i18n 盤點匯入 ✔**（2026-08-09，130 列進 `db_inprogress.meta_i18n`；
  `--check` 0/0/0、`--report` 的「① 待收斂」維持 **0**）。

