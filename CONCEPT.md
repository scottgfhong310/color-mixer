# color-mixer — 緣起與構想（CONCEPT）

> 版本 v1.0｜最後更新 2026-08-09

> 這份文件講**為什麼有這支 app**：它從哪來、解決什麼、能長成什麼。
> 「怎麼用」歸 [README](README.md)、「怎麼蓋的」歸 [DESIGN.md](DESIGN.md)、家族共同規範只標引用。

---

## 1. 發想緣起

構想是所有者一次講完的，原話（2026-08-07）：

> 1. 這一個 App 是一個「調色盤」，也是 `Art Color` 家族的一員；
> 2. 調色盤有兩個主要的功能：
>    - 2.1 我可以在畫面上調出一個顏色，我可以有這一個顏色的資訊，也可以從現在的 `Art Color` 家族中找到相近的顏色；
>    - 2.2 針對 `Art Color` 家族中的一個顏色，我可以增加一系列的「應用校準」色。
> 3. 頁面就像一張大畫布，畫布可以調出一個自己的底色，接下來，在這一個底色上可以選擇顏料混色。顏料可以設定「透明度」。

這三句話有一件事很不尋常：**它同時給了介面的形狀（第 3 點）與資料的形狀（第 2 點）**，而且兩者不衝突。
畫布的底色就是基材的紙色、顏料層就是 `tb_color` 的一支筆、透明度就是濃度——
每一個 UI 元素都對得上一個既有的資料概念。這支 app 因此沒有經歷「先做出來再想資料怎麼存」的階段。

**它也是家族第一支「消費五支 registry」的 app**（前面 `color-palette`／`thangka-trace` 各消費部分），
所以它天生站在一個位置：**手上有 1,779 支可比對的筆，卻沒有一個地方能問「把它們混起來會怎樣」。**

---

## 2. 那兩則例子，決定了整個資料模型

構想講完的隔一句，所有者補了兩則實際紀錄：

> 1. COPIC B39 在白色的 A4 紙上，感覺紙上的顏色接近 `#08093D` 但較深。
> 2. COPIC B39 在紙漿原色的宣紙上，感覺接近 `#08093D` 但較亮些、沒有渲開。

**這兩句話比整段構想更有資訊量**，因為它們暴露了三件不寫下來就會做錯的事：

1. **兩則的錨點是同一個 `#08093D`，差別只在後面那個修飾詞。**
   所以「校準值」不是一個 hex，是**兩個**：你目視比對時第一次停下的**錨點**，
   與微調之後你**認可**的值。兩者的差距是你自己的目視偏差，跨紀錄看得出有沒有系統性。
   再加上型錄值，就是 [DESIGN.md](DESIGN.md) §2「為什麼校準要存三個 hex」。
2. **「沒有渲開」不是顏色。** 它是行為事實，塞不進任何 hex 欄位。
   於是行為事實先走自由註記，等看得出模式再結構化（§2.1）——**不為了一則觀察去設計一個欄位**。
3. **「較深」在中文裡同時可以指明度更低與彩度更高。** 這件事一直到 2026-08-09 才引爆：
   兩種讀法把第一則推向**相反的方向**（與第二則分別是 ΔE00 6.97 與 3.30）。
   所有者裁決為彩度，兩天的判斷才一致。
   **目視微調的三根滑桿分成明度／彩度／色相，理由就是這個字分不出 L 與 C。**

---

## 3. 構想在接觸現實時被修正了四次

這支 app 從 init 到功能完整只有兩天、15 個 commit，但**每一步都推翻了前一步的一個假設**。
這一節記下來，是因為那些假設現在看起來都很合理——下一個人很可能重新做出同樣的假設。

### ① 「調濃度就能調到」——不成立，而且可以證明

所有者實際使用後回報：**以 B39 為基礎調濃度，怎麼樣都得不到紙上畫出來的顏色。**

量出來那不是技巧問題：濃度只能在「紙色 → 型錄色」之間插值，而目標 `#171159`（L\* 11.3）
比 B39 型錄色 `#255da7`（L\* 39.6）暗 28 個 L\*，**四個模型全部最近只到 ΔE00 25.2、且都停在滿濃度**。

⚠️ 而那個 25.2 正是治理文件早就寫著的東西——**COPIC 的 hex 來自型錄，型錄自述「印刷色 ≠ 墨水」**。
所以這一題本來就屬於 2.2 校準，不屬於 2.1 調色。**兩個功能的界線是在使用者撞到它時才顯影的。**

### ② 「拆成三原色比較好調」——對的，而且做得出來

所有者接著提的：疊色很難，因為「對於顏色的組成與混合的經驗是不足的」，
所以想把一個色**拆成 RGB 三原色再調整**。

那句話問的其實是一個數學問題，而答案是肯定的：**四個混色模型的差別只在於「在哪個空間裡插值」**，
於是疊層恆等於該空間裡的一個**凸組合**——「這組顏料調得出哪些顏色」就是一個凸包，
反解就是求最近點。有唯一最佳解，**而且到不了時能證明到不了**。

（順帶回答了同一則訊息裡的第四點：「用 CMYK 更難、B 調不出來」。實測 CMY 20.65、加 K 之後 5.30
——**那不是技巧問題，是那個調色盤少一個維度**，也正是印刷業要有 K 的理由。）

### ③ 「目視值有唯一真值」——被所有者自己推翻

所有者接著指出：目視調整本身就有挑戰——工作區的光線、螢幕的品質與色準、個人對色彩的敏銳度。

> 雖然裸視結果不科學，但是接近「一個人對於顏色的表達」。

**這句話推翻了前一天才寫進 schema 的一條唯一約束。** `rel_color_substrate` 當初帶著
`UNIQUE (色, 基材, 層數)`，那不是實作細節，是一句斷言：「這個組合有唯一一個真值。」
既然值取決於光線／螢幕／觀察者，那個真值就不存在（`db_artcolor` CHG000050 拿掉了它）。

而正確的重新定義比原本的更強：**存的不是「墨水是什麼顏色」，是「在這個螢幕上放什麼值，
看起來才像我當時看到的」。** 這樣一講，那三個變因全都在框架之內、消費時互相抵消——
於是它**不是客觀值的近似，而是主觀值的精確紀錄**。所有者說的「一個人對於顏色的表達」，
比那更準：**它就是那個表達本身。**

⚠️ 順帶量了一件更該知道的事：**這個不精確會不會被放大？不會。** 目視誤差 1:1 傳遞到答案，
從不放大——那已經是理論下限。稀疏區（家族色譜裡沒有相近色的地方）誤差 ΔE00 3 以內
甚至完全不換筆；密集區名次天天在翻，但換到的筆與原本只差你自己的誤差量級。
**該看的不是名次穩不穩，是「換到的筆長得多不一樣」。**

### ④ 「真實資料進來就好了」——真值帶進來一個完全靜默的錯

佔位資料替宣紙編了一個看起來合理的 `#f0e6d2`；DB 裡的真值是 **NULL**（兩張紙都沒量過）。
照實匯出之後，缺席的 `baseHex` 會讓 `normalizeStack` 退回 `#ffffff`——
**「紙漿原色宣紙」於是渲染成跟白 A4 一模一樣的白紙**，而罩染模型下紙色是會透出來的，
整條算出來的東西都變成白紙的答案。**零報錯。**

判準因此進了程式：**「沒有值」要用一個看得見的值表示，不能用欄位消失表示**
（匯出器輸出顯式的 `baseKnown: true|false`）。

---

## 4. 為什麼需要它

家族已經有五支色號 registry，它們各自回答「這個品牌有哪些色」。
但實際用筆的時候，問題從來不是那個，而是三個它們都答不出來的：

| 問題 | 誰答得出來 |
| --- | --- |
| 這幾支疊在這張紙上會變成什麼？ | **只有這支** |
| 我要這個顏色，用手上這幾支調得出來嗎？調不出來的話差多少？ | **只有這支**（而且能證明「調不出來」） |
| 這支筆在**我的**紙上、**我的**眼睛看起來是什麼顏色？ | **只有這支**（型錄答的是印刷品） |

第三個問題是這支 app 存在的最深理由：**registry 記的是原廠說了什麼，而畫畫的人需要知道的是它在自己手上是什麼。**

---

## 5. 能做什麼（現在）

- **基材 ＋ 最多 8 層半透明顏料 → 結果色**，顏料取自家族五支 registry（1,779 支可比對的筆）。
- **四個混色模型分兩組講清楚**：光的疊加（`sRGB 合成`／`OKLab 合成`）與減色（`罩染`＝Beer-Lambert，
  預設／`調色盤混合`＝單常數 Kubelka-Munk）。同一個透明度滑桿在四者下分別是覆蓋率／墨層厚度／濃度
  ——**畫面恆顯示，因為錯的從來不是某個做法，是不講**。
- **跨五品牌找最接近的筆**（ΔE00），且尊重各品牌自己的比對規矩；同時報**穩健度**
  （第 1 名與第 2 名差多少，決定你的目視誤差會不會改變答案）。
- **反解**：給一個目標色，回「用這組顏料怎麼疊最接近」，殘差恆顯示、到不了會明講。
- **目視校準**：錨點 ＋ 明度／彩度／色相三根滑桿，記下「這支筆在這張紙上看起來是什麼顏色」；
  同一組可以記很多次，同框架的差距是你自己的精度、跨框架的差距是另一回事，**分開報**。
- **筆刷**：塗抹試色，重疊處依當下模型加深——**一筆恆等於圓圈那個顏色**。
  這是圓圈做不到的事：圓圈一次只顯示一個顏色，筆刷同時顯示 0～n 筆的整條序列，
  **於是「四個模型不一樣」第一次看得見**。

---

## 6. 刻意不做（界線）

- **不自己算最接近色。** lib 只有 `mergeNearest`，沒有 `nearestAcross`——五個品牌的比對器各有各的規矩
  （`nearestFC` 只比 Art & Graphic、`nearestCDA` 排除 PSTC…），在這裡重寫一份就是同一條規則的第二份實作。
- **不連任何資料庫。** 校準資料由 `db_artcolor` 匯出成靜態檔，**本 app 唯讀**、零後端，
  clone 下來就能跑（比照五支色彩 registry）。DB 是建置期的 SoR，不是執行期來源。
- **不假裝減色模型是量出來的。** `罩染` 與 `調色盤混合` 都是由**單一 sRGB hex** 反推的近似——
  真正的 Kubelka-Munk 需要每個顏料實測的 K 與 S 光譜曲線，我們沒有。畫面一律標「近似」。
- **不校正光線與螢幕。** 我們沒有分光儀，也沒有螢幕的 ICC 特性化。觀測框架只回答
  「這兩次是不是在同一個框架下做的」——那是 identity 問題，不是測量值。
- **塗鴉不是 state。** 網址存的是配方（可分享、可重現），畫布是一塊試色布。

---

## 7. 未來可能長出的方向

以下都**還沒做，也還沒承諾**——列出來是因為現在的結構已經容得下它們。

- **校準資料長大之後**，能回答目前答不出來的問題：這個人的目視是不是**系統性地**偏某個方向？
  若是，那個偏移本身就是資料，可以外推到還沒記過的筆。
- **`fd_layers` 目前恆為 1。** 多層數的校準（同一支筆疊兩次、三次在紙上實際是什麼顏色）
  可以直接對照筆刷的 `paintTable`——**那會是四個模型第一次有真實世界的對照組**。
- **更多基材。** `meta_substrate` 有 `fd_kind`（paper／cloth／board），現在只有兩張紙。
- **第六個品牌。** STAEDTLER 早已滿足進庫條件；`tb_color` 的兩層模型與本 app 的品牌登記表
  都是為了「加一個品牌不必改 schema」設計的。
- **反解吃更大的調色盤。** 目前上限 8 支（`MAX_LAYERS`），凸包求解本身不受此限。

⚠️ **兩件現在就缺、而且只有所有者做得到的事**：兩張紙的紙色尚未量測（`baseKnown: false`）、
B39 在白 A4 上只有一次觀測（所以「重複性」目前回 `null` 而不是 0——
**0 會被讀成「量過而且很一致」**）。在紙色量出來之前，宣紙那條路算出來的都還是白紙的答案。
